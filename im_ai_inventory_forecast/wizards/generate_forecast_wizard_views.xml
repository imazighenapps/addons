<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <record id="view_generate_forecast_wizard_form" model="ir.ui.view">
        <field name="name">generate.forecast.wizard.form</field>
        <field name="model">generate.forecast.wizard</field>
        <field name="arch" type="xml">
            <form>
                <group>
                    <field name="config_id"/>
                    <field name="all_products"/>
                    <field name="product_ids" widget="many2many_tags" invisible="all_products"/>
                    <field name="warehouse_ids" widget="many2many_tags"/>
                </group>
                <footer>
                    <button name="action_generate" string="Generate Forecasts" type="object" class="btn-primary"/>
                    <button string="Cancel" class="btn-secondary" special="cancel"/>
                </footer>
            </form>
        </field>
    </record>

    <record id="action_generate_forecast_wizard" model="ir.actions.act_window">
        <field name="name">Generate Forecasts</field>
        <field name="res_model">generate.forecast.wizard</field>
        <field name="view_mode">form</field>
        <field name="target">new</field>
    </record>
</odoo>
