<?xml version="1.0" encoding="utf-8"?>
<odoo>

    <!-- Smart button on partner form -->
    <record id="view_partner_form_duplicate_button" model="ir.ui.view">
        <field name="name">res.partner.form.duplicate.button</field>
        <field name="model">res.partner</field>
        <field name="inherit_id" ref="base.view_partner_form"/>
        <field name="arch" type="xml">
            <xpath expr="//div[@name='button_box']" position="inside">
                <button name="action_view_duplicates"
                        type="object"
                        class="oe_stat_button"
                        icon="fa-copy"
                        invisible="duplicate_count == 0">
                    <field name="duplicate_count" widget="statinfo"
                           string="Duplicates"/>
                </button>
            </xpath>
        </field>
    </record>

</odoo>
